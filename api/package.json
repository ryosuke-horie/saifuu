{
  "name": "saifuu-api",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src/",
    "format": "biome format --write src/",
    "check": "npm run typecheck && biome check src/",
    "check:fix": "npm run typecheck && biome check --write src/"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.6",
    "@cloudflare/vite-plugin": "^1.2.3",
    "@cloudflare/workers-types": "^4.20250628.0",
    "@types/node": "^24.0.7",
    "drizzle-kit": "^0.31.4",
    "drizzle-orm": "^0.44.2",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-ssr-components": "^0.3.0",
    "wrangler": "^4.17.0"
  }
}
